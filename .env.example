# ============================================================================
# AI-Agent Configuration
# ============================================================================
# Скопируйте этот файл в .env и заполните реальными значениями
# cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# База данных PostgreSQL
# ----------------------------------------------------------------------------
DB_HOST=postgres
DB_PORT=5432
DB_NAME=ai_agent
DB_USER=postgres
DB_PASS=postgres

# Для локального запуска (без Docker):
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=ai_agent
# DB_USER=postgres
# DB_PASS=your_password_here

# ----------------------------------------------------------------------------
# OpenAI API
# ----------------------------------------------------------------------------
# Получите API ключ на https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o
OPENAI_MAX_TOKENS=4000

# Альтернативные модели:
# OPENAI_MODEL=gpt-4-turbo
# OPENAI_MODEL=gpt-4
# OPENAI_MODEL=gpt-3.5-turbo

# ----------------------------------------------------------------------------
# Логирование
# ----------------------------------------------------------------------------
# Режим работы: dev (разработка) или prod (production)
ENV=dev

# Уровень логирования: debug, info, warn, error, fatal
LOG_LEVEL=debug

# ----------------------------------------------------------------------------
# Миграции базы данных
# ----------------------------------------------------------------------------
# Путь к миграциям (относительно рабочей директории)
MIGRATIONS_PATH=file://migrations

# ----------------------------------------------------------------------------
# Playwright / Браузер
# ----------------------------------------------------------------------------
# DISPLAY для видимого браузера (X11)
# В Docker: :0
# Локально на Linux/Mac: :0 или значение из $DISPLAY
DISPLAY=:0

# Запуск браузера в headless режиме (true/false)
# false = видимый браузер (рекомендуется для разработки)
PW_HEADLESS=false

# Директория для persistent sessions браузера
# В Docker: /app/userdata
# Локально: ./userdata или ~/.cache/ai-agent-browser
PW_USER_DATA_DIR=/app/userdata

# Путь к браузерам Playwright
# В Docker: /ms-playwright
# Локально: обычно не требуется (используется по умолчанию)
PLAYWRIGHT_BROWSERS_PATH=/ms-playwright

# ----------------------------------------------------------------------------
# Приложение (опционально)
# ----------------------------------------------------------------------------
APP_NAME=AI-Agent
APP_VERSION=0.1.0
