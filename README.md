# AI-Agent

🤖 Автономный AI-агент для управления веб-браузером через CLI с использованием GPT-4o и Playwright.

## ✨ Возможности

- 🌐 Автономная навигация по веб-сайтам
- 🧠 Интеллектуальное планирование действий через GPT-4o
- 🔄 Многошаговое выполнение сложных задач
- 🎯 Специализированные подагенты (Navigation, Form, Extraction, Interaction)
- 💾 Сохранение сессий браузера (persistent mode)
- 🔒 Проверка безопасности действий с подтверждением пользователя
- 📊 Логирование всех действий в PostgreSQL
- 🎨 Красивый CLI интерфейс с цветами и историей команд

## 📋 Требования

- **Go 1.24+**
- **Docker и Docker Compose** (для PostgreSQL)
- **Playwright** (устанавливается через Go, но требует системные зависимости)
- **OpenAI API ключ**

### Системные зависимости для Playwright

#### macOS
```bash
# Playwright установится автоматически через go-playwright
# Браузер Firefox загрузится при первом запуске
```

#### Linux (Ubuntu/Debian)
```bash
# Установите зависимости для Firefox
sudo apt-get update
sudo apt-get install -y \
    libnss3 \
    libnspr4 \
    libatk1.0-0 \
    libatk-bridge2.0-0 \
    libcups2 \
    libdrm2 \
    libxkbcommon0 \
    libxcomposite1 \
    libxdamage1 \
    libxfixes3 \
    libxrandr2 \
    libgbm1 \
    libasound2
```

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd AI-Agent
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env` и заполните значения:

```bash
cp .env.example .env
```

Обязательно укажите:
```env
# OpenAI
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4o
OPENAI_MAX_TOKENS=4000

# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ai_agent
DB_USER=postgres
DB_PASS=your-secure-password

# Браузер (Firefox)
PW_HEADLESS=false                    # true для headless режима
PW_USER_DATA_DIR=./browser-data      # Папка для сохранения сессий
DISPLAY=:0                            # Для Linux

# Логирование
ENV=dev                               # dev, prod, test
LOG_LEVEL=info                        # debug, info, warn, error
```

### 3. Запуск PostgreSQL

```bash
docker-compose up -d postgres
```

Проверьте что БД запущена:
```bash
docker-compose ps
```

### 4. Установка Go зависимостей

```bash
go mod download
```

**Важно**: При первом запуске приложения `playwright-go` автоматически загрузит браузер Firefox (~100MB).

### 5. Сборка и запуск

```bash
# Сборка
go build -o ai-agent cmd/main.go

# Запуск
./ai-agent
```

Или запустите без сборки:
```bash
go run cmd/main.go
```

## 💻 Использование

### Основные команды CLI

После запуска приложения доступны следующие команды:

```bash
# Создание и управление задачами
task <текст задачи>     # Создать новую задачу
tasks                   # Показать список всех задач
run <id>                # Выполнить задачу по ID
status <id>             # Показать статус задачи
show <id>               # Детальная информация о задаче
logs <id>               # Логи выполнения задачи

# Работа с браузером
open <url>              # Открыть URL в браузере
open-persistent         # Открыть браузер для ручной настройки (логин и т.д.)

# Тестирование
test-llm <задача>       # Протестировать планирование LLM

# Утилиты
clear                   # Очистить экран
exit                    # Выход из приложения
```

### Примеры использования

#### 1. Простая задача - поиск информации
```bash
> task Найди информацию о погоде в Москве на сайте yandex.ru
✓ Создана задача #1

> run 1
▶ Запуск задачи #1: Найди информацию о погоде в Москве на сайте yandex.ru
[Агент выполняет шаги...]
✓ Задача выполнена успешно!
```

#### 2. Работа с формами
```bash
> task Зайди на example.com, заполни форму поиска запросом "test" и отправь
```

#### 3. Использование persistent режима для авторизации
```bash
# Сначала откройте браузер и вручную авторизуйтесь
> open-persistent
🌐 Запуск браузера в persistent режиме...
✓ Браузер открыт с сохранением сессии
[Вручную залогиньтесь на нужных сайтах]
⏎ Нажмите Enter для закрытия браузера...
✓ Сессия сохранена в ./browser-data

# Теперь запускайте задачи - агент будет использовать сохраненную сессию
> task Проверь уведомления в Facebook
```

## 🏗️ Архитектура проекта

```
AI-Agent/
├── cmd/
│   └── main.go                    # Точка входа приложения
├── internal/
│   ├── agent/                     # Основная логика агента
│   │   ├── agent.go               # Главный агент
│   │   ├── multistep_executor.go  # Многошаговое выполнение
│   │   ├── subagents.go           # Специализированные подагенты
│   │   ├── security.go            # Проверка безопасности действий
│   │   ├── domain_whitelist.go   # Whitelist критичных доменов
│   │   └── ...
│   ├── browser/                   # Управление браузером
│   │   ├── browser.go             # Playwright обертка (Firefox)
│   │   ├── forms.go               # Работа с формами
│   │   ├── popup_detector.go     # Детектор попапов
│   │   ├── snapshot.go            # Снимки страниц
│   │   ├── scroll.go              # Прокрутка страниц
│   │   ├── wait.go                # Ожидание элементов
│   │   └── ajax.go                # Работа с AJAX
│   ├── cli/                       # CLI интерфейс
│   │   ├── cli.go                 # Основная логика CLI
│   │   ├── commands/              # Обработчики команд
│   │   │   ├── task.go
│   │   │   ├── show.go
│   │   │   ├── logs.go
│   │   │   ├── browser.go
│   │   │   └── llm.go
│   │   ├── ui/                    # UI компоненты
│   │   │   ├── colors.go
│   │   │   ├── formatting.go
│   │   │   └── welcome.go
│   │   └── user_input.go
│   ├── config/                    # Конфигурация
│   │   └── config.go
│   ├── database/                  # Работа с БД
│   │   ├── db.go
│   │   ├── models.go
│   │   └── repository.go
│   ├── llm/                       # Интеграция с OpenAI
│   │   ├── client.go
│   │   ├── rate_limiter.go
│   │   └── prompts.go
│   ├── logger/                    # Логирование (Zap)
│   │   └── logger.go
│   ├── sanitizer/                 # Санитизация данных
│   └── migrations/                # Миграции БД
│       └── scripts/
├── docker-compose.yml             # PostgreSQL в Docker
├── .env                           # Конфигурация (не в git!)
├── .env.example                   # Пример конфигурации
└── README.md
```

## 🔒 Безопасность

Агент включает несколько уровней защиты:

1. **Rule-based проверка** - быстрый анализ опасных паттернов:
   - Финансовые операции (payment, card, checkout)
   - Удаление данных (delete, remove, trash)
   - Изменение настроек (settings, admin, config)
   - Отправка форм (submit, send, post)
   - Подтверждения (confirm, accept, agree)

2. **LLM-based проверка** - дополнительный анализ через GPT-4o

3. **Domain whitelist** - критичные домены всегда требуют подтверждения:
   - Банки (sberbank.ru, alfabank.ru, tinkoff.ru)
   - Платежные системы (paypal.com, stripe.com)
   - Криптобиржи (binance.com, coinbase.com)
   - Госуслуги (gosuslugi.ru)

4. **Блокировка админ-панелей** - автоматически блокируются:
   - /admin, /administrator
   - /wp-admin, /phpmyadmin
   - /cpanel

5. **Rate limiting** для OpenAI API:
   - 60 запросов в минуту
   - 90,000 токенов в час

## 🐛 Troubleshooting

### Ошибка подключения к БД

```bash
# Проверьте что PostgreSQL запущен
docker-compose ps
docker-compose logs postgres

# Перезапустите если нужно
docker-compose restart postgres
```

### Браузер не запускается

```bash
# Убедитесь что playwright браузеры установлены
# При первом запуске приложения они должны загрузиться автоматически

# Если проблема сохраняется, попробуйте установить вручную
go run github.com/playwright-community/playwright-go/cmd/playwright@latest install firefox

# Для Linux - установите системные зависимости (см. раздел "Требования")
```

### Ошибка "OPENAI_API_KEY обязателен"

Убедитесь что в `.env` файле указан валидный API ключ:
```env
OPENAI_API_KEY=sk-your-api-key-here
```

Ключ должен начинаться с `sk-`

### Ошибки миграций

Проверьте путь к миграциям в `.env`:
```env
MIGRATIONS_PATH=file://internal/migrations/scripts
```

### Display error на Linux

Если используете headless=false на Linux без GUI:
```bash
# Установите Xvfb
sudo apt-get install xvfb

# Запустите приложение через xvfb-run
xvfb-run -a ./ai-agent
```

Или переключитесь на headless режим в `.env`:
```env
PW_HEADLESS=true
```

## 🧪 Разработка

### Запуск для разработки

```bash
# Только PostgreSQL
docker-compose up -d postgres

# Приложение с hot reload (требует air)
go install github.com/cosmtrek/air@latest
air

# Или обычный запуск
go run cmd/main.go
```

### Логи

Логи сохраняются в:
- **dev режим**: вывод в консоль
- **prod режим**: файл `logs/app.log`

Уровни логирования:
- `debug` - детальная информация (только для dev)
- `info` - важные события
- `warn` - предупреждения
- `error` - ошибки

### База данных

Посмотреть данные в БД:
```bash
# Подключитесь к PostgreSQL
docker exec -it ai-agent-postgres psql -U postgres -d ai_agent

# Посмотрите таблицы
\dt

# Посмотрите задачи
SELECT id, user_input, status FROM tasks ORDER BY id DESC LIMIT 10;

# Посмотрите шаги задачи
SELECT step_no, action_type, target_selector, result FROM agent_steps WHERE task_id = 1;

# Выход
\q
```

## 📝 Технологии

- **Go 1.24+** - основной язык
- **Playwright (Firefox)** - автоматизация браузера
- **OpenAI GPT-4o** - планирование действий
- **PostgreSQL** - хранение задач и логов
- **GORM** - ORM для базы данных
- **Zap** - структурированное логирование
- **Readline** - история команд в CLI
